"use client";
import { useCallback,useEffect,useMemo,useState } from "react";
import { supabase } from "../../lib/supabase";
import { useWorkspace } from "../../hooks/useWorkspace";
import { usePermissao } from "../../hooks/usePermissao";
import { ModuloBloqueado,useModulos } from "../../hooks/useModulos";
import MetaSettings from "./MetaSettings";
import { Alertas } from "./MetaDashboardViews";
import MetaExecutiveDashboard from "./MetaExecutiveDashboard";
import MetaCampaignReport from "./MetaCampaignReport";
import { Dashboard,ganhou,InstagramDashboard,n,noIntervalo,PADRAO,PERIODOS,Preferencias,Proposta,RegraAlerta,StatusData } from "./metaTypes";
import styles from "./MetaAds.module.css";
const META_BASE=process.env.NEXT_PUBLIC_META_URL||"https://meta.api.wolfgyn.com.br",FB_APP_ID=process.env.NEXT_PUBLIC_META_FB_APP_ID||"922442717546553",GRAPH_VERSION=process.env.NEXT_PUBLIC_META_GRAPH_VERSION||"v21.0";
const erroTexto=(e:unknown,f:string)=>e instanceof Error&&e.message?e.message:f;
export default function MetaAdsPage(){
 const{wsId,wsPronto}=useWorkspace(),{isDono,isSuperAdmin,perfil}=usePermissao(),podeConfigurar=isDono||isSuperAdmin||perfil==="Administrador",{modulos,carregado:modulosCarregados}=useModulos();
 const[status,setStatus]=useState<StatusData|null>(null),[dashboard,setDashboard]=useState<Dashboard|null>(null),[instagram,setInstagram]=useState<InstagramDashboard|null>(null),[preferencias,setPreferencias]=useState<Preferencias>(PADRAO),[propostas,setPropostas]=useState<Proposta[]>([]),[periodo,setPeriodo]=useState("last_30d"),[dataInicio,setDataInicio]=useState(()=>new Date().toISOString().slice(0,10)),[dataFim,setDataFim]=useState(()=>new Date().toISOString().slice(0,10)),[secao,setSecao]=useState<"dashboard"|"campanhas">("dashboard"),[configurando,setConfigurando]=useState(false),[alertasAbertos,setAlertasAbertos]=useState<RegraAlerta[]>([]),[carregando,setCarregando]=useState(true),[atualizando,setAtualizando]=useState(false),[conectando,setConectando]=useState(""),[erro,setErro]=useState(""),[mensagem,setMensagem]=useState("");
 const api=useCallback(async(path:string,init:RequestInit={})=>{const{data:{session}}=await supabase.auth.getSession();if(!session?.access_token)throw new Error("Sua sessão expirou. Entre novamente.");const response=await fetch(META_BASE+"/ads"+path,{...init,headers:{"Content-Type":"application/json",Authorization:"Bearer "+session.access_token,...(init.headers||{})},cache:"no-store"});const data=await response.json().catch(()=>({}));if(!response.ok||data.sucesso===false)throw new Error(data.erro||"Não foi possível consultar a Meta.");return data},[]);
 const carregarPropostas=useCallback(async()=>{if(!wsId)return;const desde=new Date();desde.setMonth(desde.getMonth()-13);const{data}=await supabase.from("proposta").select("created_at,data_proposta,status_venda,valor_plano").eq("workspace_id",wsId).gte("created_at",desde.toISOString()).limit(10000);setPropostas((data||[])as Proposta[])},[wsId]);
 const carregarInstagram=useCallback(async(p=periodo)=>{if(!wsId)return;const datas=p==="custom"?"&desde="+dataInicio+"&ate="+dataFim:"";try{setInstagram(await api("/instagram/dashboard?workspaceId="+encodeURIComponent(wsId)+"&periodo="+p+datas))}catch{setInstagram(null)}},[api,dataFim,dataInicio,periodo,wsId]);
 const carregarDashboard=useCallback(async(forcar=false,p=periodo)=>{if(!wsId)return;if(p==="custom"&&(!dataInicio||!dataFim||dataInicio>dataFim)){setErro("Escolha uma data inicial anterior à data final.");return}setAtualizando(true);setErro("");const datas=p==="custom"?"&desde="+dataInicio+"&ate="+dataFim:"";try{const data=await api("/dashboard?workspaceId="+encodeURIComponent(wsId)+"&periodo="+p+datas+(forcar?"&atualizar=1":""));setDashboard(data);if(status?.instagram?.conectado)await carregarInstagram(p)}catch(e){setErro(erroTexto(e,"Falha ao carregar anúncios."));setDashboard(null)}finally{setAtualizando(false)}},[api,carregarInstagram,dataFim,dataInicio,periodo,status?.instagram?.conectado,wsId]);
 const carregarStatus=useCallback(async()=>{if(!wsId)return;setCarregando(true);setErro("");try{const[dadosStatus,dadosPrefs]=await Promise.all([api("/status?workspaceId="+encodeURIComponent(wsId)),api("/preferencias?workspaceId="+encodeURIComponent(wsId))]);setStatus(dadosStatus);setPreferencias(dadosPrefs.preferencias||PADRAO);await carregarPropostas();if(dadosStatus.conectado&&dadosStatus.contaSelecionada)setDashboard(await api("/dashboard?workspaceId="+encodeURIComponent(wsId)+"&periodo="+periodo));if(dadosStatus.instagram?.conectado)try{setInstagram(await api("/instagram/dashboard?workspaceId="+encodeURIComponent(wsId)+"&periodo="+periodo))}catch{setInstagram(null)}}catch(e){setErro(erroTexto(e,"Falha ao verificar a conexão com a Meta."))}finally{setCarregando(false)}},[api,carregarPropostas,periodo,wsId]);
 useEffect(()=>{if(wsPronto&&modulosCarregados&&modulos.meta_ads)carregarStatus()},[wsPronto,modulosCarregados,modulos.meta_ads,carregarStatus]);
 const abrirOAuth=(tipo:"ads"|"instagram")=>{if(!podeConfigurar){setErro("Somente o dono ou administrador pode conectar contas.");return}const state=crypto.randomUUID(),redirectUri=window.location.origin+"/crm/marketing/meta-ads",scope=tipo==="instagram"?"ads_read,business_management,pages_show_list,pages_read_engagement,instagram_basic,instagram_manage_insights":"ads_read,business_management",url=new URL("https://www.facebook.com/"+GRAPH_VERSION+"/dialog/oauth");url.search=new URLSearchParams({client_id:FB_APP_ID,redirect_uri:redirectUri,response_type:"token",scope,state,display:"popup",auth_type:"rerequest"}).toString();const popup=window.open(url.toString(),"wolf-meta-"+tipo,"popup=yes,width=640,height=780,resizable=yes,scrollbars=yes");if(!popup){setErro("O navegador bloqueou a janela da Meta. Libere os pop-ups.");return}setConectando(tipo);setErro("");let encerrado=false;let intervalo=0,limite=0;const finalizar=()=>{window.removeEventListener("message",receber);window.clearInterval(intervalo);window.clearTimeout(limite)};const receber=async(event:MessageEvent)=>{if(event.origin!==window.location.origin||event.data?.type!=="wolf-meta-ads-oauth"||event.data?.state!==state)return;encerrado=true;finalizar();popup.close();if(event.data?.error||!event.data?.accessToken){setConectando("");setErro(event.data?.error||"A conexão foi cancelada.");return}try{const endpoint=tipo==="instagram"?"/instagram/conectar":"/conectar",data=await api(endpoint,{method:"POST",body:JSON.stringify({workspaceId:wsId,accessToken:event.data.accessToken})});setMensagem(data.mensagem||"Conta conectada.");await carregarStatus()}catch(e){setErro(erroTexto(e,"Não foi possível conectar a conta."))}finally{setConectando("")}};window.addEventListener("message",receber);intervalo=window.setInterval(()=>{if(popup.closed&&!encerrado){encerrado=true;finalizar();setConectando("")}},500);limite=window.setTimeout(()=>{if(!encerrado){encerrado=true;finalizar();popup.close();setConectando("");setErro("A conexão demorou demais.")}},120000)};
 const selecionarPortfolio=async(id:string)=>{if(!id)return;setAtualizando(true);setDashboard(null);setErro("");try{const data=await api("/selecionar-portfolio",{method:"POST",body:JSON.stringify({workspaceId:wsId,businessId:id})});setMensagem(data.mensagem||"Portfólio selecionado.");await carregarStatus()}catch(e){setErro(erroTexto(e,"Não foi possível selecionar o portfólio."))}finally{setAtualizando(false)}};
 const selecionar=async(tipo:"ads"|"instagram",id:string)=>{if(!id)return;setAtualizando(true);if(tipo==="ads")setDashboard(null);try{const path=tipo==="ads"?"/selecionar-conta":"/instagram/selecionar",chave=tipo==="ads"?"accountId":"instagramUserId";await api(path,{method:"POST",body:JSON.stringify({workspaceId:wsId,[chave]:id})});await carregarStatus()}catch(e){setErro(erroTexto(e,"Não foi possível selecionar a conta."))}finally{setAtualizando(false)}};
 const salvarPreferencias=async()=>{try{const data=await api("/preferencias",{method:"PUT",body:JSON.stringify({workspaceId:wsId,preferencias})});setPreferencias(data.preferencias);setConfigurando(false);setMensagem(data.mensagem)}catch(e){setErro(erroTexto(e,"Não foi possível salvar as preferências."))}};
 const moeda=dashboard?.conta?.moeda||status?.contaSelecionada?.moeda||"BRL",money=useMemo(()=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:moeda}),[moeda]),inteiro=useMemo(()=>new Intl.NumberFormat("pt-BR",{maximumFractionDigits:0}),[]),formatar=(v:number,t:string)=>t==="moeda"?money.format(v||0):t==="percentual"?n(v).toLocaleString("pt-BR",{maximumFractionDigits:2})+"%":t==="vezes"?n(v).toLocaleString("pt-BR",{maximumFractionDigits:2})+"x":t==="decimal"?n(v).toLocaleString("pt-BR",{maximumFractionDigits:2}):inteiro.format(v||0);
 const propostasResumo=useMemo(()=>{if(!dashboard)return{atual:0,anterior:0,vendas:0,receita:0,variacao:0};const data=(p:Proposta)=>p.data_proposta||p.created_at,atual=propostas.filter(p=>noIntervalo(data(p),dashboard.periodo.atual)),anterior=propostas.filter(p=>noIntervalo(data(p),dashboard.periodo.anterior)),vendas=atual.filter(p=>ganhou(p.status_venda));return{atual:atual.length,anterior:anterior.length,vendas:vendas.length,receita:vendas.reduce((t,p)=>t+n(p.valor_plano),0),variacao:anterior.length?((atual.length-anterior.length)/anterior.length)*100:null}},[dashboard,propostas]);
 const mensal=useMemo(()=>dashboard?.serieMensal.map(item=>{const prefix=String(item.data).slice(0,7),props=propostas.filter(p=>String(p.data_proposta||p.created_at||"").slice(0,7)===prefix),vendas=props.filter(p=>ganhou(p.status_venda));return{...item,mes:new Date(item.data+"T12:00:00").toLocaleDateString("pt-BR",{month:"short",year:"2-digit"}),propostas:props.length,vendasCrm:vendas.length,receitaCrm:vendas.reduce((t,p)=>t+n(p.valor_plano),0)}})||[],[dashboard,propostas]);
 useEffect(()=>{if(!dashboard||!preferencias.regrasAlerta.length){setAlertasAbertos([]);return}const regras=preferencias.regrasAlerta.filter(r=>{if(!r.ativo)return false;const v=n((dashboard.resumo as unknown as Record<string,number>)[r.metrica]);return r.operador==="maior"?v>r.valor:r.operador==="menor"?v<r.valor:r.operador==="maior_igual"?v>=r.valor:v<=r.valor});setAlertasAbertos(regras);if(regras.length&&preferencias.somAtivo&&wsId){const chave="meta-alertas-"+wsId+"-"+new Date().toISOString().slice(0,10)+"-"+regras.map(r=>r.id).join("-");if(!localStorage.getItem(chave))try{const Ctx=window.AudioContext||window.webkitAudioContext,ctx=new Ctx(),osc=ctx.createOscillator(),gain=ctx.createGain();osc.frequency.value=regras.some(r=>r.nivel==="critico")?310:regras.some(r=>r.nivel==="sucesso")?740:520;gain.gain.setValueAtTime(.12,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+.55);osc.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+.55);localStorage.setItem(chave,"1")}catch{}}},[dashboard,preferencias.regrasAlerta,preferencias.somAtivo,wsId]);
 if(!modulosCarregados)return <div className={styles.loading}>Verificando acesso...</div>;if(!modulos.meta_ads)return <ModuloBloqueado modulo="meta_ads"/>;if(carregando)return <div className={styles.loading}>Preparando sua Central Ads...</div>;
 return <div className={styles.centralShell}><aside className={styles.adsSidebar}><div className={styles.sideBrand}><div>📊</div><span><b>Central Ads</b><small>Mídia & performance</small></span></div><nav><button className={secao==="dashboard"?styles.sideActive:""} onClick={()=>setSecao("dashboard")}><span>⌂</span><div><b>Dashboard</b><small>Visão executiva</small></div></button><button className={secao==="campanhas"?styles.sideActive:""} onClick={()=>setSecao("campanhas")}><span>▥</span><div><b>Campanhas</b><small>Custos e relatórios</small></div></button></nav><div className={styles.sideFooter}><span>● Somente leitura</span><small>A Wolf não altera seus anúncios.</small></div></aside><main className={styles.adsContent}>
  <header className={styles.header}><div><span className={styles.readOnly}>CENTRAL ADS</span><h1>{secao==="dashboard"?"Dashboard de resultados":"Relatório de campanhas"}</h1><p>{secao==="dashboard"?"Uma leitura completa e simples da sua operação de mídia.":"Compare custos, resultados e eficiência campanha por campanha."}</p></div><div className={styles.actions}>{status?.conectado&&<button className={styles.secondary} onClick={()=>carregarDashboard(true)} disabled={atualizando}>{atualizando?"Atualizando...":"↻ Atualizar dados"}</button>}{podeConfigurar&&<button className={styles.secondary} onClick={()=>setConfigurando(true)}>⚙ Configurações e alertas</button>}</div></header>
  {erro&&<div className={[styles.notice,styles.noticeError].join(" ")}><b>Atenção:</b> {erro}</div>}{mensagem&&<div className={[styles.notice,styles.noticeOk].join(" ")}>{mensagem}</div>}{preferencias.popupAtivo&&alertasAbertos.length>0&&<Alertas regras={alertasAbertos} resumo={dashboard?.resumo} money={money} fechar={()=>setAlertasAbertos([])}/>}
  {!status?.conectado?<section className={styles.connectBox}><div><h2>Conecte o Gerenciador de Anúncios</h2><p>A Wolf consulta campanhas e métricas sem alterar anúncios, públicos ou orçamentos.</p></div>{podeConfigurar?<button className={styles.primary} onClick={()=>abrirOAuth("ads")} disabled={!!conectando}>{conectando==="ads"?"Conectando...":"Conectar com a Meta"}</button>:<span>Aguardando administrador</span>}</section>:<><div className={styles.toolbar}><div className={styles.account}><div className={styles.metaIcon}>M</div><div><b>{status.contaSelecionada?.nome||"Escolha uma conta"}</b><small>{status.metaUsuario?.nome} · Portfólio: {status.portfolioSelecionado?.nome||"não selecionado"}{status.contaSelecionada?.id?" · "+status.contaSelecionada.id:""} · Fuso: {status.contaSelecionada?.fusoHorario||"da conta Meta"}</small></div></div><div className={styles.actions}>{((status.portfolios?.length||0)>1||!status.portfolioSelecionado)&&<select value={status.portfolioSelecionado?.id||""} onChange={e=>selecionarPortfolio(e.target.value)} disabled={atualizando}><option value="">Portfólio empresarial...</option>{status.portfolios?.map(p=><option key={p.id} value={p.id}>{p.nome}{p.quantidadeContas?" ("+p.quantidadeContas+")":""}</option>)}</select>}{status.portfolioSelecionado&&((status.contas?.length||0)>1||!status.contaSelecionada)&&<select value={status.contaSelecionada?.id||""} onChange={e=>selecionar("ads",e.target.value)} disabled={atualizando}><option value="">Conta de anúncios...</option>{status.contas?.map(c=><option key={c.id} value={c.id}>{c.nome}</option>)}</select>}<select value={periodo} onChange={e=>{const valor=e.target.value;setPeriodo(valor);if(valor!=="custom")carregarDashboard(false,valor)}}>{PERIODOS.map(([v,l])=><option key={v} value={v}>{l}</option>)}<option value="custom">Período personalizado</option></select>{periodo==="custom"&&<div className={styles.customDates}><label>De<input type="date" value={dataInicio} max={dataFim} onChange={e=>setDataInicio(e.target.value)}/></label><label>Até<input type="date" value={dataFim} min={dataInicio} onChange={e=>setDataFim(e.target.value)}/></label><button className={styles.primary} onClick={()=>carregarDashboard(true,"custom")} disabled={atualizando}>{atualizando?"Buscando...":"Aplicar período"}</button></div>}</div></div>{!status.contaSelecionada?<div className={styles.empty}>{status.portfolioSelecionado?"Escolha uma conta de anúncios deste portfólio para começar.":"Escolha o portfólio empresarial que este workspace poderá acompanhar."}</div>:dashboard?<>{secao==="dashboard"?<MetaExecutiveDashboard dashboard={dashboard} resumo={dashboard.resumo} preferencias={preferencias} money={money} inteiro={inteiro} formatar={formatar} mensal={mensal} propostas={propostasResumo} instagram={instagram}/>:<MetaCampaignReport dashboard={dashboard} money={money} inteiro={inteiro} formatar={formatar}/>}<footer className={styles.footer}>Atualizado em {new Date(dashboard.atualizadoEm).toLocaleString("pt-BR")} · dados fornecidos pela Meta · dados comerciais identificados como CRM Wolf</footer></>:<div className={styles.empty}>Atualize os dados para abrir o painel.</div>}</>}
  {configurando&&<MetaSettings prefs={preferencias} setPrefs={setPreferencias} salvar={salvarPreferencias} fechar={()=>setConfigurando(false)}/>}</main></div>;
}
declare global{interface Window{webkitAudioContext:typeof AudioContext}}
